TODO list:

\begin{itemize}
    %\item Make correlation maps for the features.
    %\item Boxplot and histogram for general consumption, boxplots per month (like in \cite{khan_ensemble_2021} and 30 in \cite{moon_advancing_2024}), and possibly by week
    \item It is possible to make diagrams for the decomposition of the time series \cite{chakraborty_scenario-based_2021}.
    \item Could investigate correlation between external variables and consumption like in \cite{deb_forecasting_2016}
          %\item Could I incorporate seasonal index? \cite{cao_support_2016} shows how to calculate SI
          %\item Could I do group clustering for consumers? \cite{khan_ensemble_2021}
    \item Maybe relevant to measure time for training models?
\end{itemize}

This study aims to develop monthly electricity consumption forecasts for residential households, with a focus on forecast explainability. A variety of input variables, including historical electricity consumption, household information, weather variables, and calendar information, are used to provide diverse information to the forecasting models. Five decision tree-based ensemble learning models are utilized for the forecasting, and their predictive performance as well as their explainability is assessed and compared. The explainable AI method \emph{SHAP} is used to investigate the effect and importance of the model features. \autoref{fig:methodology-diagram} shows the general architecture of building the explainable electricity consumption forecasts.

\begin{figure}[h!tbp]%[ht]
    \centering
    \includegraphics[width=1\linewidth]{figures/methodology_diagram.pdf}
    \caption{Architecture of modelling explainable electricity consumption forecasts [fix this figure].}
    \label{fig:methodology-diagram}
\end{figure}

\section{Data Preparation and Analysis}

This study utilizes electricity consumption data from Umeå Energi, along with external variables in the form of weather data and holiday information. This section describes the data, presents analytical insights as part of enhancing the explainability of the forecasts, and details how the data is preprocessed before performing forecasts.

\subsection{Electricity Consumption Data}

The Umeå Energi dataset contains monthly electricity consumption from 14 286 Swedish households, consisting of 4 years of measurements retrieved from January 1st 2022 to November 30th 2025. This data is provided by Swedish electricity company Umeå Energi. For the purpose of this research, households are defined as unique customer and facility combinations (i.e., one customer may have several facilities, which are regarded as separate households). Households that have any undefined or less than 10 kWh consumption measurements during the time period are removed from the dataset, leaving 13 692 households with complete measurements. All customers are private customers located in Umeå in the northern part of Sweden, meaning the data represents household electricity consumption in a cold climate. The dataset is summarized in \autoref{tab:dataset-statistics}, and \autoref{fig:kwh_histogram_2022_2025} shows the distribution of the electricity consumption for all households during the entire time period.

\begin{table}[H]%[ht]
    \centering
    \caption{Summary of the Umeå Energi electricity consumption data.}
    \label{tab:dataset-statistics}
    \small
    \begin{tabularx}{\linewidth}{l r}
        \textbf{Statistic}   & \textbf{Umeå Energi Data (kWh)} \\
        \midrule
        Minimum              & 10                              \\
        First quartile       & 124                             \\
        Median               & 220                             \\
        Mean                 & 465                             \\
        Third quartile       & 489                             \\
        Maximum              & 13 490                          \\
        \midrule
        Time range           & 2022-01-01 to 2025-11-30        \\
        Location             & Umeå, Sweden                    \\
        Number of households & 13 692                          \\
    \end{tabularx}
\end{table}

\begin{figure}[H]%[ht]
    \centering
    \includegraphics[width=1\linewidth]{figures/kwh_histogram.pdf}
    \caption{Histogram over the monthly electricity consumption distribution from January 2022 to November 2025.}
    \label{fig:kwh_histogram_2022_2025}
\end{figure}

\autoref{fig:kwh_histogram_2022_2025} and \autoref{tab:dataset-statistics} show that a vast majority of households consume electricity in a similar low range per month, but that there are outliers where the monthly consumption exceeds several thousand kWh. \autoref{fig:avg_consumption_2022_2025} shows the average monthly consumption for all households. It is clear that there is a significant seasonal pattern in the consumption, with more electricity being consumed in the winter and less in the summer. This makes sense for the cold climate, since the winter requires more heating for households with electrical heating, while summers are cool enough to not require much electrical cooling. A warmer climate may have the opposite consumption pattern, since warm summers will have a higher need for cooling measures [source]. \autoref{fig:avg_consumption_per_month} illustrates the average consumption by month, showing that consumption varies more during the winter months, while the average consumption is more stable during the summer.

\begin{figure}[H]%[ht]
    \centering
    \includegraphics[width=1\linewidth]{figures/avg_consumption.pdf}
    \caption{Average household electricity consumption from January 2022 to November 2025.}
    \label{fig:avg_consumption_2022_2025}
\end{figure}

\begin{figure}[H]%[ht]
    \centering
    \includegraphics[width=1\linewidth]{figures/avg_consumption_month.pdf}
    \caption{Average household electricity consumption by month from January 2022 to November 2025.}
    \label{fig:avg_consumption_per_month}
\end{figure}

Three input features are constructed from the historical electricity consumption; $lag\_1$ which represents the consumption of the previous month for the target household, $lag\_12$ which represents the consumption for the same month the previous year for the target household, and $rolling\_mean\_3$ which represents the average consumption over the past three months for the target household. These variables express the recent consumption patterns, but also the monthly dynamic. Since the year 2022 does not have any past year historical consumption to refer to, it is removed from the training data.

\subsubsection{Household Information}

In addition to the historical electricity consumption, the Umeå Energi data contains information about the households. Households are labeled by type of residence, type of heating system, and type of contract agreement with Umeå Energi. The type of residence is either house, apartment, or vacation home. Heating can be electrical, non-electrical, or district heating. Contract agreements may be variable, fixed, or default assigned contracts. The share of households in each category is found in \autoref{tab:household-specifications}. These features are not directly maintained by Umeå Energi, but instead, the labeling is done by Umeå Energi based on certain properties of the customer, which may not always be 100\% accurate. However, it provides a good estimation of the household properties. In order to use the properties as input to the forecasting models, the string values are encoded as integers. [skriv om att dessa kan ändras under tiden]

\begin{table}[H]%[ht]
    \centering
    \caption{Household types in the Umeå Energi dataset.}
    \label{tab:household-specifications}
    \small
    \begin{tabularx}{\linewidth}{l r}
        \textbf{Type}          & \textbf{Percentage} \\
        \midrule
        Apartment              & 58\%                \\
        House                  & 38\%                \\
        Vacation home          & 4\%                 \\
        \midrule
        Non-electrical heating & 70\%                \\
        Electrical heating     & 19\%                \\
        District heating       & 11\%                \\
        \midrule
        Variable contract      & 82\%                \\
        Default contract       & 18\%                \\
        Fixed contract         & $<1\%$              \\
    \end{tabularx}
\end{table}

\subsubsection{Household Clustering Using K-Means}

Since there are many households in the electricity consumption data, it is relevant to not only group households by their properties, but also by their consumption patterns. The idea is to identify groups where the similarity within the group is higher than that among the groups \cite{khan_ensemble_2021}. K-Means clustering is one of the most popular algorithms for solving this problem in a simple way. K-Means is an unsupervised learning algorithm, where the main idea is to select $k$ centroids, one for each cluster, and place them as far from each other as possible. The next step is to associate each point in the dataset with the closest centroid. After associating all points, the $k$ centroids are recalculated by averaging the points within each cluster. This process is repeated until the centroids no longer move. Formally, the algorithm aims at minimizing the squared error function $W(S,C)$ defined in \autoref{equ:k-means} \cite{kodinariya_review_2013}.

\begin{equation}\label{equ:k-means}
    W(S,C) = \sum_{k=1}^{K} \sum_{i \in S_k}^{} \left\| y_i - c_k \right\|^2
\end{equation}

In the equation, $S$ is a partition of $k$ clusters represented by observations in the form of vectors $y_i$. Each cluster $S_k$ is non-empty and non-overlapping, and has a centroid $c_k$ ($k = 1,2,\dots,K)$.

In these experiments, K-Means clustering is applied to define customer groups based on their electricity consumption. The elbow method is used to find an optimal number of $k$, i.e. the number of clusters. The elbow method is a visual method, where different values of $k$ are plotted against the variance within the clusters \cite{cui_introduction_2020}. As the value of $k$ increases, the average variance becomes smaller. The goal is to find a value of $k$ for which the variance stops decreasing sharply, that is, the elbow of the graph. \autoref{fig:elbow_plot} shows the elbow plot for determining the optimal number of customer groups, in this case $k=3$. This results in three customer groups; low consumption, medium consumption, and high consumption. Each customer in the dataset is assigned a label based on the cluster it belongs to, and this label becomes an input feature for the forecasting models. \autoref{tab:cluster-profiles} details each cluster regarding size and consumption patterns. The large standard deviation present in each cluster suggests that outlier months with high consumption exist for all identified consumption patterns.

\begin{figure}[H]
    \centering
    \includegraphics[width=0.75\linewidth]{figures/elbow_plot.pdf}
    \caption{Elbow plot for finding the optimal number of customer profile clusters $k$.}
    \label{fig:elbow_plot}
\end{figure}

\begin{table}[H]%[ht]
    \centering
    \caption{Clustered consumption profiles in the Umeå Energi dataset.}
    \label{tab:cluster-profiles}
    \small
    \begin{tabularx}{\linewidth}{l r r r r}
        \textbf{Cluster}   & \textbf{Households} & \textbf{Mean (kWh)} & \textbf{Median (kWh)} & \textbf{SD (kWh)} \\
        \midrule
        Low consumption    & 1 804               & 365                 & 173                   & 515               \\
        Medium consumption & 4 458               & 410                 & 227                   & 549               \\
        High consumption   & 7 305               & 524                 & 230                   & 724               \\
    \end{tabularx}
\end{table}

\subsubsection{Seasonal Index}

[dont know if i will actually be using this, but if so, write here.  To represent the seasonal component of monthly forecasting, a seasonal index is calculated based on the electricity consumption. försöka använda detta i samband med clustering]

\subsubsection{Variable Interactions}

Interactions between the variables are investigated using a correlation map, as shown in \autoref{fig:variable-correlation-map}. This correlation map includes the internal historical consumption features and SI, as well as external features in the form of weather indices (THI, WCT [see section x]) and calendar information (number of holidays, number of days). The three historical consumption variables show a high correlation with each other, while they have a low correlation with all the external variables and the seasonal index. [conclusions from this?]

\begin{figure}[H]%[ht]
    \centering
    \includegraphics[width=1\linewidth]{figures/variable_correlation_map.pdf}
    \caption{Correlation map of variables [placeholder].}
    \label{fig:variable-correlation-map}
\end{figure}

\subsection{External Variables}

Along with the historical electricity consumption data from Umeå Energi, weather variables are retrieved from SMHI\footnote{Swedish Meteorological and Hydrological Institute, SMHI. \url{https://www.smhi.se/data} (Accessed 2026-03-17)} and calendar information is retrieved from Dagsmart API\footnote{Swedish calendar data API, Dagsmart API. \url{https://dagsmart.se/api/} (Accessed 2026-03-16)}. These external variables are intended to improve the forecasts by including environmental factors that may affect consumption patterns.

\subsubsection{Weather Data}

Weather data is retrieved from SMHI's open data for the January 1st 2022 to November 30th 2025 time period. Nine variables are selected from the Umeå Airport measurement station, while three variables pertaining to solar attributes are retrieved from the Umeå Sol station, and snow depth is retrieved from Umeå Röbäcksdalen. All variables are obtained as hourly measurements, except snow depth which is retrieved at daily intervals. \autoref{tab:weather-variables} details the weather variables and their measurement units. [Across all variables, 4.9\% of the measurements are missing, where most missing values are from the lowest cloud base variable (87.0\% of the missing values).] These measurements are aggregated to monthly values using mean. \autoref{fig:weather-plot-monthly} shows the monthly values of each weather variable plotted over the entire time range. [maybe put plot in appendix?] [need to discuss how these values are aggregated (avg vs sum or other way)]. The seasonal component is apparent for many of the weather variables, while some variables such as wind speed and air pressure lack a seasonal pattern. [conclusions from this]

\begin{table}[H]%[ht]
    \centering
    \caption{Weather variables retrieved from SMHI.}
    \label{tab:weather-variables}
    \small
    \begin{tabularx}{\linewidth}{l l l}
        \textbf{Variable}     & \textbf{Description}                          & \textbf{Resolution} \\
        \midrule
        $temperature$         & Air temperature (\unit{\degreeCelsius})       & Hourly              \\
        $dew\_point\_temp$    & Dew point temperature (\unit{\degreeCelsius}) & Hourly              \\
        $snow\_depth$         & Snow depth (m)                                & Daily               \\
        $humidity$            & Relative humidity (\%)                        & Hourly              \\
        $wind\_direction$     & Wind direction (\unit{\degreeCelsius})        & Hourly              \\
        $wind\_speed$         & Wind speed (m/s)                              & Hourly              \\
        $lowest\_cloud$       & Lowest cloud base (m)                         & Hourly              \\
        $global\_radiation$   & Global irradiance (Sweden) (W/m$^2$)          & Hourly              \\
        $longwave\_radiation$ & Long wave irradiance (W/m$^2$)                & Hourly              \\
        $sunshine$            & Sunshine amount (s)                           & Hourly              \\
        $air\_pressure$       & Air pressure (hPa)                            & Hourly              \\
        $visibility$          & Visibility (m)                                & Hourly              \\
    \end{tabularx}
\end{table}

\begin{figure}[H]
    \centering
    \includegraphics[width=1\linewidth]{figures/weather_plot.pdf}
    \caption{Weather variables' monthly values between January 2022 and November 2025.}
    \label{fig:weather-plot-monthly}
\end{figure}

%totalt saknas 16646 värden, där moln är 14475 av dessa (totala antalet measurements är 34320*10)


Could be relevant to include some variable(s) of minimum and maximum temperature? As in \cite{lazzari_user_2022}

Although weather factors have been proven to be useful for predicting electricity consumption, the link between weather and electricity consumption can be indistinct. Weather indices incorporating temperature, humidity, and wind speed can provide a better representation of the perceived environmental effects and the impact on electricity consumption \cite{moon_advancing_2024}. Two indices, temperature humidity index (THI) and wind chill temperature (WCT), are calculated as in \autoref{equ:THI} and \autoref{equ:WCT}. In the equations, $temp$ is temperature, $humi$ is humidity, and $ws$ is wind speed. These indices are included as input features for the forecasting models.

\begin{equation}\label{equ:THI}
    THI = (1.8 \times temp + 32) - [(0.55 - 0.0055 \times humi) \times (1.8 \times temp - 26)]
\end{equation}

\begin{equation}\label{equ:WCT}
    WCT = 13.12 + 0.6215 \times temp - 11.37 \times ws^{0.16} + 0.3965 \times temp \times ws^{0.16}
\end{equation}

\subsubsection{Calendar Information}

In addition to weather data, calendar information is included as external factors. During holidays, energy consumption behaviors change due to differences in social patterns. Energy consumption is not only dependent on the decision-making of individuals, it also relies on the cultural context of social and economic activities \cite{roman_holidays_2015}. Holiday information is retrieved from Dagsmart, an open Swedish calendar information API. All holidays and weekend days during the years 2022 through 2025 are obtained, and these are compiled to a \emph{number of holidays} feature that indicates the number of holidays for each month in the dataset. In \autoref{fig:holidays-per-month} the number of holidays per month is visualized, showing that December is month with the most holidays. The fewest holidays are during the rest of the winter and autumn months. An additional \emph{number of days} feature is constructed for each month in the dataset, since the number of days varies (e.g., February is missing several days of consumption compared to months with 31 days).

\begin{figure}[H]
    \centering
    \includegraphics[width=1\linewidth]{figures/holidays_per_month.pdf}
    \caption{Number of Swedish holidays per month during the years 2022 through 2025.}
    \label{fig:holidays-per-month}
\end{figure}

\subsection{Feature Selection}

The historical consumption, weather, and calendar variables are compiled to a complete feature set for the forecasting models. A feature selection is made from the weather variables, based on the correlation map found in \autoref{fig:weather-correlation-map}. Variables with a high correlation to several other variables are excluded from the feature set, in this case [variables]. This is due to highly correlated features accounting for each other's impact to the model output [formulera bättre], and the SHAP method assuming that features are uncorrelated \cite{salih_perspective_2025}. The complete selected feature set is found in \autoref{tab:input-variables}. The data is split into training and test data such that the years 2023 and 2024 are the training set, and January through November 2025 is the test data (see \autoref{tab:train-test-split}).

\begin{table}[H]%[ht]
    \centering
    \caption{Train-test split of the dataset.}
    \label{tab:train-test-split}
    \small
    \begin{tabularx}{\linewidth}{l l r}
        \textbf{Data} & \textbf{Time range}           & \textbf{Percentage} \\
        \midrule
        Training data & January 2023 to December 2024 & 69\%                \\
        Test data     & January 2025 to November 2025 & 31\%                \\
    \end{tabularx}
\end{table}

\section{Ensemble Learning Forecasting Models}

Forecasting is performed using different decision tree-based ensemble learning models. Decision trees classify data items by asking a sequence of questions about the items' features. The nodes of the tree contain questions, and for each possible answer, every internal node points to one child node. The sorting of an item into a class begins from the root of the tree, and according to the item's answer to the questions, it follows the path to a leaf node \cite{kingsford_what_2008}. [Possibly a diagram of a decision tree] One large advantage with decision trees is that they are considerably easier to interpret than neural networks, as they follow logical rules instead of having connections with numeric weights between nodes. Decision trees perform well for a few highly relevant attributes, but not as well if there are many attributes with complex interactions. Ensembles of decision trees can be employed to increase accuracy when the training data is more complex and changing \cite{kotsiantis_decision_2013}. Decision tree-ensembles are considered among the most reliable and precise methods for regression and classification tasks, and they have been shown to be effective for predicting energy consumption \cite{moon_advancing_2024}. However, one disadvantage is that the interpretability decreases when multiple classifiers are involved, making it more difficult to understand the reasoning behind the ensemble's decision \cite{kotsiantis_decision_2013}. This creates a need for model explainability outside the ensemble's inner workings, which in this thesis is addressed by applying explainable AI. [move to theory?]

Five decision tree-based ensemble models are selected as forecasting models, as in \cite{moon_advancing_2024}; random forest (RF), gradient boosting machine (GBM), extreme gradient boosting machine (XGBoost), light gradient boosting machine (LightGBM), and categorical boosting (CatBoost). [GBM and CatBoost were best, so focus on them first]

Random Forest \cite{breiman_random_2001}

Gradient boosting machines are a family of ML algorithms where the learning process consecutively fits a new tree with the goal of reducing the errors of the previous one. This is done by constructing new base-learners that are maximally correlated to a decreasing loss function. The loss function can be of various kinds, which makes GBM highly flexible for different data-driven tasks. It is up to the model designer to choose the most appropriate loss function \cite{natekin_gradient_2013}. GBM is able to learn complex patterns and interactions in the data due to its iterative learning process \cite{moon_advancing_2024}. [Possibly a diagram showing its structure]

XGBoost \cite{chen_xgboost_2016}

LightGBM is another gradient boosting algorithm that focuses on efficiency for cases with many input features and when the amount of data is large.  \cite{ke_lightgbm_2017}

CatBoost is a gradient boosting algorithm that uses a technique called \emph{ordered boosting} to reduce prediction shift caused by target leakage which occurs in other gradient boosting algorithms. CatBoost also encodes categorical features uniquely, by applying a technique called \emph{ordered target statistics} to handle high cardinality features \cite{prokhorenkova_catboost_2018}. Because of this, CatBoost is able to achieve high accuracy, especially when the data contains a significant number of categorical features \cite{moon_advancing_2024}. [Possibly a diagram showing its structure]

[GridSearchCV to do grid search]

\section{Evaluation Metrics}
To evaluate the predictive performance of the forecasting models, three statistical metrics are used; mean absolute percentage error (MAPE), root mean squared error (RMSE), and mean absolute error (MAE). MAPE is one of the most common metrics to evaluate forecasting accuracy, due to its scale-independence and interpretability. It defines the average of the absolute percentage errors, as in \autoref{equ:mape}. One disadvantage to be aware of is that MAPE yields infinite or undefined values when the actual values are zero or close to zero \cite{kim_new_2016}. This experiment handles this by removing any households from the dataset that have less than 10 kWh consumption during some month. RMSE and MAE, as defined in \autoref{equ:rmse} and \autoref{equ:mae}, are utilized to give insights on the size of the errors in kWh. These two metrics are also among the most commonly used in the electricity consumption forecasting field \cite{nti_electricity_2020}.

%To evaluate the predictive performance of the forecasting models, three statistical metrics are used; mean absolute percentage error (MAPE), coefficient of variation of the root mean squared error (CVRMSE), and normalized mean absolute error (NMAE). MAPE is one of the most common metrics to evaluate forecasting accuracy, due to its scale-independence and interpretability. It defines the average of the absolute percentage errors, as in \autoref{equ:mape}. One disadvantage to be aware of is that MAPE yields infinite or undefined values when the actual values are zero or close to zero \cite{kim_new_2016}. This is handled by removing any households from the dataset that have less than 10 kWh consumption during some month. [Maybe use RMSE and MAE instead? Those give answers in kWh, which could also be insightful. \cite{nti_electricity_2020} says MAPE, RMSE and MAE are the most common.] These metrics express the performance in error percentage, making them intuitive to interpret.

\begin{equation}\label{equ:mape}
    MAPE = \frac{1}{n} \times \sum \left| \frac{y_t - \hat{y}_t}{y_t} \right| \times 100
\end{equation}

\begin{equation}\label{equ:rmse}
    RMSE = \sqrt{\frac{1}{n} \times \sum (y_t - \hat{y}_t)^2}
\end{equation}

\begin{equation}\label{equ:mae}
    MAE = \frac{1}{n} \times \sum \left| y_t - \hat{y}_t \right|
\end{equation}

\begin{comment}
\begin{equation}\label{equ:cvrmse}
    CVRMSE = (\sqrt{\frac{1}{n} \times \sum (Y_t - \hat{Y}_t)^2}) / \ddot{Y} \times 100
\end{equation}

\begin{equation}\label{equ:nmae}
    NMAE = (\frac{1}{n} \times \sum \left| Y_t - \hat{Y}_t \right|) / \ddot{Y} \times 100
\end{equation}
\end{comment}

In the equations, $y_t$ denotes the actual observed value at time $t$, while $\hat{y}_t$ denotes the predicted value at time $t$.
%$\ddot{Y}$ represents the mean of all observed values, and 
The total number of prediction instances is denoted by $n$.

\begin{comment}
To evaluate the models in a more comprehensive way, the harmonic mean (HM) is calculated based on these three metrics. The harmonic mean will favor models that perform well in all three metrics, while models that perform exceptionally well in one metric but poorly in the others will be valued less. This is due to the harmonic mean being effective at mitigating extreme outliers, making it suitable for combining differing metrics \cite{moon_advancing_2024}. The HM is calculated as in \autoref{equ:harmonic-mean}.

\begin{equation}\label{equ:harmonic-mean}
    HM = \frac{3}{(1/MAPE) + (1/CVRMSE) + (1/NMAE)}
\end{equation}
\end{comment}

\section{Explainable AI Analysis}

To enhance the interpretability of the forecasts and investigate the influence of model features, a SHAP analysis is performed. The SHAP methodology [Explain SHAP for ensemble models specifically, like in \cite{moon_advancing_2024}, explain why SHAP gives better insights than just feature importances (\url{https://machinelearningmastery.com/a-gentle-introduction-to-shap-for-tree-based-models/})] [beeswarm plot, feature importance plot]

This experiment uses the SHAP \emph{TreeExplainer}, specifically designed for decision tree models.

\section{Experimental Setup}

Experiments are performed in the Python language. The \emph{scikit-learn} package provides the RF and GBM models, while XGBoost, LightGBM, and CatBoost are obtained from [deras egna repon typ?]. [borde jag nämna pandas, seaborn...?] The \emph{shap} package is used for the explainable AI analysis. Experiments are run in a virtual environment using a [GPU].